# Native ALOE ViT **base** (B-cos) + ImageNet-1k linear-probe checkpoints.

defaults:
  - /model/backbone/google_vit/aloe_vit_base_local_weights@_here_
  - _self_

num_labels: 1000
aloe_use_logit_layer: true
linear_probe_checkpoint: "${oc.env:CACHE_PATH}/aloe-align-once-to-explain/d0z6m7v6/checkpoints/best_lr=0.001.ckpt"
