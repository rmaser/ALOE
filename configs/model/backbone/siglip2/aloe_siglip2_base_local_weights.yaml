defaults:
  - /model/backbone/siglip2/aloe_siglip2_base@_here_
  - _self_

# Trunk from a local Lightning checkpoint (no Hub weights).
_target_: src.models.config.Siglip2Config
load_pretrained_weights: false
backbone_checkpoint: "${oc.env:CACHE_PATH}/aloe-align-once-to-explain/ukol400t/checkpoints/last.ckpt"
