defaults:
  - /model/backbone/siglip2/siglip2_so400m@_here_
  - _self_

# Native ALOE SigLIP2 **SO400M** (B-cos): architecture only — **no** Hub or local checkpoint weights.
_target_: src.models.config.Siglip2Config
name: "rmaser/aloe-siglip2-so400m"
is_bcos: true
trust_remote_code: false
aloe_bcos_impl: v2
load_pretrained_weights: false
