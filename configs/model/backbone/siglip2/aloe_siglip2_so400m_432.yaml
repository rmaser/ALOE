defaults:
  - /model/backbone/siglip2/siglip2_so400m_432px@_here_
  - _self_

# Native ALOE SigLIP2 **SO400M @432px** (B-cos): architecture only — **no** Hub weights.
_target_: src.models.config.Siglip2Config
name: "rmaser/aloe-siglip2-so400m-432"
is_bcos: true
trust_remote_code: false
aloe_bcos_impl: v2
load_pretrained_weights: false
