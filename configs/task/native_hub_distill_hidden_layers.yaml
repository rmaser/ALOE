# @package _global_
#
# Three hidden layers + pooler via ``select_distillation_layers`` (matches teacher depth).
#
model:
  cfg:
    distillation_layers: ${select_distillation_layers:${backbone.student.layers}}
